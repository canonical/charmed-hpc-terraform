# Slurm cluster deployment in Juju

This example shows how to deploy a Slurm cluster using Juju with MySQL as the backing database.

## Usage

To run this example, execute:

```bash
terragrunt init
terragrunt apply
```
